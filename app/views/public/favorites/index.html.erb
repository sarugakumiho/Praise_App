<div class="container">
  <h4 class="text-center py-2 mb-4" style="font-weight: 600; font-size: 1.5rem; background-color: #c6e2ff; color: #000000; border-radius: 12px; box-shadow: 0px 3px 6px rgba(0, 0, 0, 0.1); letter-spacing: 0.5px;">
    いいねした投稿
  </h4>
  
  <div class="row">
    <% if @favorited_posts.any? %>
      <% @favorited_posts.each do |post| %>
        <div class="col-md-4 mb-4 d-flex">
          <div class="card h-100 d-flex flex-column" style="min-width: 100%; max-width: 100%;">
            <%= link_to post_path(post), class: "d-flex flex-column" do %>
              <!-- 画像部分 -->
              <div style="height: 200px; background-color: #f0f0f0; display: flex; align-items: center; justify-content: center; width: 100%;">
                <% if post.image.attached? %>
                  <%= image_tag post.image, class: "card-img-top", style: "height: 200px; object-fit: cover; width: 100%;" %>
                <% else %>
                  <span style="color: #888;">No Image</span>
                <% end %>
              </div>
              
              <!-- テキスト部分 -->
              <div class="card-body d-flex flex-column">
                <h5 class="card-title" style="font-size: 1.2rem; font-weight: bold;"><%= post.title %></h5>
                <p class="card-text flex-grow-1"><%= truncate(post.memo, length: 100) %></p>
              </div>
            <% end %>
          </div>
        </div>
      <% end %>
    <% else %>
      <p class="text-center w-100">まだいいねした投稿はありません。</p>
    <% end %>
  </div>
</div>